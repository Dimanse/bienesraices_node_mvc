extends ./layout/app



block contenido

        section(class="py-5")
            h2(class="font-extrabold text-4xl text-center pt-10")= titulo

            if propiedades.length 
                div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
                    each propiedad in propiedades 
                        include ./templates/card
            
            else 
                p(class="text-center text-indigo-600 font-extrabold uppercase bg-blue-300 border-indigo-600 border-2 flex-auto py-1 px-16 mx-auto w-fit mt-5" ) No hay resultados